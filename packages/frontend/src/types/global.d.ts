/**
 * Global type declarations for the Unified Repository Analyzer frontend
 */

// Ensure DOM types are available
// Window and Document interfaces are provided by lib.dom.d.ts
// No need to redeclare them

// Import types from @types/node that might be needed
// These are automatically available when @types/node is installed as a devDependency
// NodeJS.Timeout is now available from @types/node

// Add any other global types as needed
